package defeatedcrow.hac.core.mixin;

// @Mixin(FoodData.class)
public class FoodDataMixin {

	// @ModifyVariable(method = "tick", at = @At(value = "STORE", ordinal = 0))
	// public Difficulty onTick(Difficulty dif) {
	// return dif == Difficulty.PEACEFUL ? Difficulty.EASY : dif;
	// }

}
